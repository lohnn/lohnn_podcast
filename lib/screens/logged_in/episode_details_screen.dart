import 'package:flutter/material.dart';

class EpisodeDetailsScreen extends StatelessWidget {
  final String episodeId;

  const EpisodeDetailsScreen(this.episodeId, {super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
